apiVersion: v1
kind: ConfigMap
metadata:
  name: taskapp-config
  namespace: taskapp
data:
  DATABASE_HOST: taskapp-postgres
  DATABASE_PORT: "5432"
  DATABASE_NAME: taskapp
  TASKAPP_SEED_DEFAULT_USERS: "true"
